logging:
  level:
    root: WARN
    com.jeferro: INFO


components:
  kafka:
    products:
      topic: com.jeferro.products.products.test

  rest:
      security:
        issuer: "products"
        duration: 
        password: "test"
